import { CortarUsuarioPipe } from './cortar-usuario.pipe';

describe('CortarUsuarioPipe', () => {
  it('create an instance', () => {
    const pipe = new CortarUsuarioPipe();
    expect(pipe).toBeTruthy();
  });
});
